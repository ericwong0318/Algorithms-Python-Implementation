# from data_structure import close_addressing_hash_table as ht
# from data_structure import open_addressing_hash_table as ht

import unittest

if __name__ == '__main__':
    unittest.main
    #     # HT = ht.CloseAddressingHashTable(20, 3)
    #     # HT.chained_hash_insert(1)
    #     # HT.chained_hash_insert(1)
    #     # HT.chained_hash_insert(1)
    #     # HT.chained_hash_insert(2)
    #     # print(HT.chained_hash_search(3))
    #     # HT.chained_hash_delete(1)
    #     # for val in HT.ht:
    #     #     print(val)
    #
    #     ht = ht.OpenAddressingHashTable()
    #     for i in range(30):
    #         ht.hash_insert(i)
    #     # for i in range(10):
    # l = []
    # for i in range(10):
    #     l.append(i)
    # print(l[::-1])
    # print(l[:0:-1])
